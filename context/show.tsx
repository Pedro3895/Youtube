import { createContext, useContext, useState, } from "react";
import Header from '../components/Header';
import useLocation from 'react-router-dom';
import * as React from 'react';


//export const ShowElement = createContext({} as any);



export {};
